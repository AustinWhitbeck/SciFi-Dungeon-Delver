import { Link } from "react-router-dom"


const BackButton = () => {

    return(
        <main>
            <Link to="/"><button>Back</button></Link>
        </main>
    )
}

export default BackButton;